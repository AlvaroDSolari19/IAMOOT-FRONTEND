const LoginPage = () => {
    return <div>
        <h1>Login Page</h1>
        <p>Contains a form with email and password and a button below it to submit.</p>
    </div>
};

export default LoginPage; 